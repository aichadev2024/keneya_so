# Kènèya Sô — dépendances de production
# Voir docs/Kenya_So_Cahier_des_charges_enrichi.docx, section 8 (Architecture technique)

Django==5.2.17
djangorestframework==3.18.1
django-environ==0.14.0
django-cors-headers==4.9.0
drf-spectacular==0.30.0
psycopg[binary]==3.3.5      # pilote PostgreSQL (base de données cible, CDC 8)
Pillow==12.3.0              # ImageField (logo établissement, pièces jointes imagerie)
whitenoise==6.12.0          # service des fichiers statiques en production
gunicorn==26.2.0            # serveur WSGI (déploiement Render/Railway, CDC 8)

# --- Statistiques et exports PDF/Excel (phase 9, CDC 2.2 / 9) ---
# ReportLab plutôt que WeasyPrint : pur Python, aucune bibliothèque système
# (Pango/Cairo/GDK-Pixbuf) — voir apps/core/exports.py pour le détail.
reportlab==5.0.1
openpyxl==3.1.5
arabic-reshaper==3.0.1      # liaison des lettres arabes pour les exports PDF
python-bidi==0.6.11         # inversion visuelle droite-à-gauche (arabe) en PDF
